
# src\osd\windows\window.cpp

	// make the window title
	if (video_config.numscreens == 1)
		sprintf(window->m_title, "%s: %s [%s]", emulator_info::get_appname(), machine.system().type.fullname(), machine.system().name);
	else
		sprintf(window->m_title, "%s: %s [%s] - Screen %d", emulator_info::get_appname(), machine.system().type.fullname(), machine.system().name, index);


改为


	// make the window title
	if (video_config.numscreens == 1)
		sprintf(window->m_title, "%s", machine.system().name);
	else
		sprintf(window->m_title, "%s - Screen %d", machine.system().name, index);


// %s  =  字符串占位符
// emulator_info::get_appname()  =  模拟器名称
// machine.system().type.fullname()  =  载入的游戏全名
// machine.system().name  =  载入的游戏rom名
// index  =  屏幕索引号